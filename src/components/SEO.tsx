/**
 * SEO.tsx — DEPRECATED
 *
 * All schema generation logic from this file has been moved to:
 * - NewsMediaOrganization + WebSite → layout.tsx (root layout)
 * - NewsArticle + BreadcrumbList + FAQPage + Speakable → [category]/[slug]/page.tsx
 * - CollectionPage → [category]/page.tsx, /city/[slug]/page.tsx, /tags/[tag]/page.tsx
 *
 * This component previously returned null in App Router (dead code).
 * Kept only as a reference for schema structure — do not import or render.
 */

const SEO = () => null;
export default SEO;
